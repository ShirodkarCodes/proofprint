# Genblaze — pinned to exact PyPI versions.
# NOTE: GitHub release tags (v0.5.x–v0.7.x) do NOT correspond to PyPI versions.
# 0.4.5 is the latest umbrella release actually published to PyPI.
genblaze[nvidia,google,parquet]==0.4.5

# Reads C2PA Content Credentials so the verifier can also say something useful
# about assets minted elsewhere (DALL-E, Firefly, Photoshop, C2PA cameras).
c2pa-python==0.37.2
# Pillow needs defusedxml to parse XMP, which is where editors record edit history.
defusedxml==0.7.1

fastapi==0.128.4
uvicorn[standard]==0.42.0
python-multipart==0.0.20
